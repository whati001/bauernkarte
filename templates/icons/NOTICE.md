Icons in this directory are from [Lucide](https://lucide.dev)
(lucide-static v1.31.0), ISC licensed. Vendored (not CDN-loaded) per this
project's convention; license comment kept here once instead of repeated
in every file. `class="icon"` and no explicit width/height were added on
top of the upstream files — sizing is done via CSS (`app.css`'s `.icon`
rule) so icons scale with their surrounding text.
